# Exercise 2: Add Credentials and Notifications

1. Add a Docker Hub credential in Jenkins (Manage Jenkins > Credentials)
2. Update the Build stage to push the image to Docker Hub
3. Add a post-failure step that sends a notification (email or Slack webhook)
4. Break a test and verify the notification fires
