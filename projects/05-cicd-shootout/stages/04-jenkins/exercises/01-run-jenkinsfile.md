# Exercise 1: Run the Jenkinsfile

1. Start Jenkins via Docker
2. Create a Pipeline job pointing to the Jenkinsfile
3. Run the pipeline and verify all three stages pass
4. Compare the Jenkins UI output with GitHub Actions and GitLab CI
