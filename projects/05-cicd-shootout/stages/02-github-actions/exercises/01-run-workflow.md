# Exercise 1: Run the GitHub Actions Workflow

1. Push the PipelineAPI code to a GitHub repository
2. Open the Actions tab — verify all three jobs pass
3. Break a test intentionally and push — verify the build job is skipped
4. Fix the test and push — verify the full pipeline passes
