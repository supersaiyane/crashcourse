# Exercise 1: Run Tests and Build the Image

1. Run pytest locally — all 6 tests should pass
2. Run ruff check — no issues
3. Build the Docker image using the multi-stage Dockerfile
4. Run the container and test the health endpoint
5. Inspect the image size: `docker images pipelineapi:local`
