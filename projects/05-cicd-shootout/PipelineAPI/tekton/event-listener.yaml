apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: pipelineapi-listener
spec:
  serviceAccountName: tekton-triggers-sa
  triggers:
    - name: github-push
      bindings:
        - ref: github-push-binding
      template:
        ref: pipelineapi-template
