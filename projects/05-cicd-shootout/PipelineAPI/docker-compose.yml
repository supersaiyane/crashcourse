version: "3.9"

services:
  api:
    build: ./api
    ports: ["5000:5000"]
    environment:
      FLASK_ENV: development
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 10s
      timeout: 3s
      retries: 3
