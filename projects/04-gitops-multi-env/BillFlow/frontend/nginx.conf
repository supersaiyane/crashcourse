server { listen 80; root /usr/share/nginx/html; index index.html;
  location /api/ { proxy_pass http://billflow:3000/api/; proxy_set_header Host $host; }
  location /health { proxy_pass http://billflow:3000/health; }
}
